# ci-15 — CHANGELOG.md seed + workflow audit for the v0.13.0 cut

Owner: @@CI
Phase: 8, Round 3
Date cut: 2026-05-23

## Goal

Two related deliverables to make the v0.13.0 cut (the
public-flip release) shippable:

1. **`CHANGELOG.md`** at repo root with v0.12.0 + earlier
   tagged releases summarised in Keep-a-Changelog format.
2. **Workflow audit + release-pipeline final
   verification**: review `.github/workflows/*.yml` for
   dead steps, outdated patterns, redundant matrix entries,
   missing concurrency limits. Produce a short report at
   task tail.

## Background

Round-3 Track 3 (cleanup / release readiness) per
[`../architect/round-3-plan.md`](../architect/round-3-plan.md).
v0.13.0 ships at end of Round 3 (the public-flip version,
per @@Alex's 2026-05-23 decision; NOT v1.0).

The release pipeline has been exercised end-to-end on
`chan-v0.12.0` (release.yml + release-desktop.yml both
green on first fire). This task locks the pipeline in
for v0.13.0 and adds the user-facing CHANGELOG (chan
has never had one; private-repo convention was
"journals + git log are the audit trail" — that doesn't
work for outside contributors).

## Acceptance criteria

### CHANGELOG.md

* Keep-a-Changelog format
  (https://keepachangelog.com/en/1.1.0/).
* Entries per tag in reverse-chronological order:
  v0.12.0 → v0.11.2 → v0.11.1 → v0.11.0 → v0.10.x
  (skip if minor) → older minor cuts.
* v0.12.0 entry: cite the chan-v0.12.0 annotated-tag
  message verbatim or paraphrase tight (Drafts +
  Team features + Hybrid Nav + right-click menus +
  screensaver + cursor saga). The annotated message
  already exists; reuse its prose.
* Per-entry sections: `### Added` / `### Changed` /
  `### Fixed` / `### Removed` where relevant.
* `## [Unreleased]` section at top, empty.
* Entries link to GitHub compare URLs (e.g.
  `[v0.12.0]: https://github.com/fiorix/chan/compare/chan-v0.11.2...chan-v0.12.0`).
  These work once the repo flips public.

### Workflow audit

Review `.github/workflows/*.yml`:

* `ci.yml` — per-PR gate. Ubuntu + macOS matrix
  (Windows out per `ci-13`). Look for: dead `env:`
  entries, redundant `actions/checkout` steps,
  unpinned action versions (`actions/checkout@v4` is
  pinned; spot-check the rest).
* `release.yml` — chan CLI release on `chan-v*` /
  `v*` tags. Last fired green on chan-v0.12.0
  (run 26332086305). Verify the `if:` extension
  from `ci-14` + the VERSION-strip fix are in HEAD;
  flag any divergence.
* `release-desktop.yml` — chan-desktop release on
  `chan-v*` tags. Same green on chan-v0.12.0
  (run 26332086302). Verify the download-pattern +
  upload-files broadening from `ci-14` are in HEAD.
* Any other workflow files: read + verify
  intentionality.

Report at task tail: each workflow + findings + any
fix recommendations (no fixes in this task; cut a
follow-up if needed).

## How to start

1. `git tag --list 'chan-v*' 'v*' | sort -V` → enumerate
   tags to summarise.
2. `git log --oneline <prev-tag>..<tag>` per pair to
   extract changes; group by Added/Changed/Fixed.
3. Read the chan-v0.12.0 annotated-tag message for the
   v0.12.0 entry seed.
4. Draft CHANGELOG.md.
5. Workflow audit: read each `.github/workflows/*.yml`;
   note findings.
6. Report appended to this task file tail.

## Coordination

* No release cut in this task — v0.13.0 ships
  separately at Round-3 close (gated on
  `architect-3` + `systacean-43` + this CHANGELOG +
  any release-blocker findings from frontend / backend
  cleanup tasks).
* @@Architect drafts the v0.13.0 annotated-tag message
  separately; this task's CHANGELOG entry for v0.13.0
  lands as part of that cut commit, not this task.
* Workflow audit findings that ARE release-blockers
  get fixed in a follow-up `ci-N` task; non-blockers
  land in the report.

Time-boxed: one CHANGELOG draft + one workflow audit
report. Polish lands at v0.13.0 cut time, not this
task.

## 2026-05-23 - CHANGELOG draft + workflow audit

### CHANGELOG.md

Added root [`../../../CHANGELOG.md`](../../../CHANGELOG.md)
in Keep-a-Changelog shape:

* `## [Unreleased]` placeholder at top.
* Reverse chronological entries for `v0.12.0`, `v0.11.2`,
  `v0.11.1`, `v0.11.0`, `v0.10.1`, `v0.9.0`, `v0.8.1`,
  `v0.7.1`, and `v0.6.23`.
* v0.12.0 seeded from the annotated `chan-v0.12.0` tag
  message, compressed into Keep-a-Changelog buckets.
* v0.11.2 and v0.11.1 seeded from their annotated tag
  messages.
* v0.11.0 and older minor cuts are deliberately terse
  where the tags only carry version-bump / maintenance
  metadata.
* Compare links point at GitHub tag ranges and will resolve
  cleanly after the repo flips public.

### Workflow inventory

Only three workflow files exist:

| Workflow | Purpose | Audit result |
|---|---|---|
| `.github/workflows/ci.yml` | Per-PR / main gate | OK |
| `.github/workflows/release.yml` | chan CLI release | OK |
| `.github/workflows/release-desktop.yml` | chan-desktop release | OK |

YAML parse check:

```text
.github/workflows/ci.yml: ok
.github/workflows/release.yml: ok
.github/workflows/release-desktop.yml: ok
```

### ci.yml audit

Current shape matches the intended Round-3 gate:

* Triggers: `push` to `main`, `pull_request`,
  `workflow_dispatch`.
* Rust gate: `rustfmt`; clippy + test on
  `ubuntu-latest` and `macos-latest`; no-default-features
  build on Ubuntu.
* Web gate: `npm ci`, `npm run check`, `npm run test`,
  `npm run build`.
* Windows remains out per `ci-13`; comments point to the
  phase-8 bug umbrella for re-enable.
* Linux GTK/Tauri deps are installed in both workspace cargo
  jobs, matching the `ci-12` fix for `glib-sys` /
  `webkit2gtk-sys`.
* Actions are pinned to major versions:
  `actions/checkout@v4`, `actions-rust-lang/setup-rust-toolchain@v1`,
  `Swatinem/rust-cache@v2`, `actions/setup-node@v4`.

Finding: no release blocker. Optional future improvement:
add `concurrency` to cancel superseded branch runs once PR
traffic matters; today the repo's multi-agent push cadence
makes older red runs useful audit anchors, so I do not
recommend changing it in this task.

### release.yml audit

Current shape matches the `ci-14` release fix:

* Triggers include both legacy `v*` and phase-8 `chan-v*`.
* Release-job `if:` matches both
  `refs/tags/v` and `refs/tags/chan-v`.
* VERSION staging strips both `chan-v` and `v`, so
  `chan-v0.12.0` produces `0.12.0`.
* Active build matrix ships:
  `x86_64-unknown-linux-gnu`,
  `aarch64-unknown-linux-gnu`, and
  `aarch64-apple-darwin`.
* Windows matrix entries remain documented but deferred.
* Embedded-model cache/fetch steps are hard-skipped with
  explanatory comments because no matrix entry builds
  `--features embed-model`.
* Tooling installs use prebuilt actions where previous
  task work swapped out slow `cargo install` paths.

External verification:

* `Release` run `26332086305` on `chan-v0.12.0` completed
  `success` on commit `5ed3c61`.
* Jobs all green: Ubuntu clippy + test, macOS clippy +
  test, Linux x86_64 build, Linux aarch64 build, macOS
  aarch64 build, GitHub release.
* `chan-v0.12.0` GitHub Release assets include CLI
  `.tar.gz`, `.deb`, `.rpm`, `.pkg`, plus `VERSION` and
  `SHA256SUMS`.

Finding: no release blocker. `fail_on_unmatched_files:
false` is still intentional here because the artifact list is
matrix-dependent and includes optional package shapes.

### release-desktop.yml audit

Current shape matches the `ci-14` desktop-release fix and the
new chan-desktop team boundary:

* Trigger is `chan-v*` only, plus `workflow_dispatch`.
* Active matrix is Ubuntu Linux x86_64 unsigned and macOS
  aarch64 signed.
* Apple Developer ID signing path consumes the six secrets
  from the `ci-3` brief and fails fast when any are missing.
* Notarization diagnostic upload includes build log, notary
  log JSON, bundle dir, and transporter logs.
* macOS DMG `_x64` suffix is stripped post-build.
* Release job downloads `chan-desktop-*` artifacts and uploads
  `.dmg`, `.deb`, and `.AppImage` with
  `fail_on_unmatched_files: true`.
* Windows Authenticode and Linux package signing remain
  documented future work.

External verification:

* `Release (chan-desktop)` run `26332086302` on
  `chan-v0.12.0` completed `success` on commit `5ed3c61`.
* Jobs all green: macOS build, Ubuntu build, GitHub release.
* `chan-v0.12.0` GitHub Release assets include
  `Chan_0.11.2.dmg`, `Chan_0.11.2_amd64.AppImage`, and
  `Chan_0.11.2_amd64.deb`.

Finding: no release blocker in this task's scope. Follow-up
candidate for the chan-desktop lane / @@CI bridge: the
chan-desktop artifact version is still `0.11.2` on the
`chan-v0.12.0` release assets. That looks like desktop package
metadata, not release workflow plumbing. Because
`release-desktop.yml` is now chan-desktop-owned shared infra
per the 2026-05-23 lane-boundary poke, I am flagging it here
instead of editing it: route through @@Architect / @@Alex /
@@Desktect if it should block v0.13.0.

### Commit readiness

Files changed by this task:

* `CHANGELOG.md`
* `docs/journals/phase-8/ci/ci-15.md`
* `docs/journals/phase-8/alex/event-ci-architect.md`

Verification performed:

* `ruby -ryaml -e 'YAML.load_file(...)'` on all three
  workflows: OK.
* `gh api` checked release runs `26332086305` and
  `26332086302`: both completed `success` on `chan-v0.12.0`.
* `gh api` checked `chan-v0.12.0` release assets: CLI Linux
  packages, macOS CLI package, desktop DMG, desktop Linux
  AppImage/deb, `VERSION`, and `SHA256SUMS` present.

Proposed commit subject:

```text
docs: seed CHANGELOG and audit release workflows (ci-15)
```

No workflow edits landed. Standing by for @@Architect review /
commit clearance.

## 2026-05-23 — @@Architect: approved + commit clearance

Reviewed `CHANGELOG.md` (180 lines, Keep-a-Changelog format) + the workflow-audit findings at the task tail. Both clean.

* CHANGELOG.md: tag coverage v0.12.0 → v0.6.23, v0.12.0 entry seeded from the annotated tag message and compressed into Added/Changed/Fixed buckets, compare links point at GitHub tag ranges that resolve once the repo flips public, terser sections for older minor cuts.
* Workflow audits: all three files (`ci.yml`, `release.yml`, `release-desktop.yml`) verified externally against the actual `chan-v0.12.0` runs (both green). No release blockers.
* The desktop-artifact-version finding (`Chan_0.11.2.*` filenames on the `chan-v0.12.0` release assets) is the right call to flag-not-fix — that's chan-desktop package metadata, not workflow plumbing. Routing the follow-up to @@Desktect via the cross-team bridge in a separate poke.

### Suggested commit subject

```
docs: seed CHANGELOG.md + ci-15 workflow audit (Round-3 release readiness)
```

### Commit instructions

Per the standing pre-authorization for your lane:

* Per-path `git add` only (multi-agent worktree; @@FullStackA has uncommitted chan-server work in flight on `crates/chan-server/src/routes/files.rs` per `-96` sub-pass 4; do not touch).
* Files to stage explicitly: `CHANGELOG.md`, `docs/journals/phase-8/ci/ci-15.md`, `docs/journals/phase-8/alex/event-ci-architect.md`.
* Pre-commit `git diff --staged --stat` + post-commit `git show --stat HEAD` per the atomic-audit pattern.

### Lane state post-`-15`

Queue-empty. Standing by until the v0.13.0 cut beat or any follow-up. The chan-desktop artifact-version finding routes to @@Desktect; not your follow-up.

## 2026-05-23 - teardown-complete

TEARDOWN directive received from @@Architect. CI lane is clean:

* No background `gh workflow run` / release-pipeline watcher
  from this session.
* No throwaway drives expected or created by this lane.
* `ci-15` was committed by @@Architect in `e221463`
  (`docs: land orphaned @@CI + @@Systacean session work...`),
  then folded into the v0.13.0 release-docs commit
  `75f97f3`.
* Queue empty. Stand-down FINAL.

teardown-complete
