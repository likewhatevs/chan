# Phase 4 - sparse bug-bounty placeholder (numbering skip)

Status: sparse, never ran as a worked phase
Span: no development span of its own (see Duration)

## What phase 4 actually is

Phase 4 is best described as a numbering skip with a thin bug-bounty
placeholder. It was intended as a bug-bounty / random-updates bucket but
never accumulated substantive content: no `request.md`, no `journal.md`,
no `summary.md`, and no task files. The released phase sequence jumps
from phase 3 to phase 5. The directory exists only because an early
process template and an empty bug bucket were imported on 2026-05-18.

This entry is deliberately short. Padding it to resemble the worked
phases would misrepresent the history.

## Initial asks

There is no recorded request. The closest statement of intent is the
import note (now [raw/import-note.md](raw/import-note.md)): "Phase 4 was a
bug-bounty / random-updates placeholder that never accumulated
substantive content", and "`bugs.md` -- empty when imported; the
bug-bounty bucket never received entries."

## Team, profiles, and coordination

No agents were assigned and no work was recorded. The imported
[raw/process.md](raw/process.md) defines a roster and a coordination
scheme (architect plus backend, frontend, webtest, syseng, rustacean;
all coordination through markdown files in the phase directory, ownership
by `@@mention`, task files named `{agent}-{n}.md`). Those are template
role definitions, not a record of anyone working this phase, and
`process.md` predates the per-author-directory layout adopted in phase 7.

## Duration

No development span. The only git activity touching the directory is the
single 2026-05-18 migration commit, and there are no dated headers in any
of the three files.

## Highlights, lowlights, and feedback

Not applicable: nothing was executed, so there is no work to assess. The
one observation worth surfacing is documentation drift, not agent
performance: the top-level journals index previously described phase 4 as
"missing", "import pending", and as having "never had a directory", all
of which the same-day import note already contradicted. This report and
the refreshed index correct that.

## What shipped, tried, and undone

Nothing shipped under the phase 4 name. The directory holds only an
early-form process template, an empty bug bucket, and the import note.
The work that would have happened here was absorbed into adjacent phases.

## Raw material

- Import note (why the directory exists):
  [raw/import-note.md](raw/import-note.md)
- Early process / coordination template: [raw/process.md](raw/process.md)
- Empty bug bucket: [raw/bugs.md](raw/bugs.md)
