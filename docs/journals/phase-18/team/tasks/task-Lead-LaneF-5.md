# task Lead -> LaneF (5): Wave-3 GO (partial) - fold-17 + scrubs; HOLD deletions

@@Alex chose "Commit + run Wave-3 now". The 6 code commits are landed
(c9ea3c56 editor .. 3a6623a0 shared). skills/ cut is CONFIRMED (@@Alex left the
FYI default). Proceed with the SAFE/reversible Wave-3 work now; HOLD the
irreversible deletions + the phase-18 fold-in for my explicit FINAL go.

## DO NOW (safe, reversible)
1. Fold phase-17 -> docs/phases/phase-17.md (closed phase; your 15/16 synthesis
   method) + add its README entry.
2. Scrubs B-1..10 from task-LaneF-Lead-2.md (the .rs/.js/.yml/.md comment
   repoints). connecting.js (item 6) is now UNBLOCKED - @@LaneE's desktop work
   committed at 2e429f27. CHANGELOG: the ONE stale "docs/journals" line only.
3. coordination.md: stage the rewrite (Edits 1-3 + the em-dash conversion per
   task-Lead-LaneF-4) and POKE ME THE `git diff docs/coordination.md` for
   sign-off BEFORE you commit it (my guardrail 3).
4. Commit the fold-17 + scrubs as docs commits (your call on grouping; pathspec
   + post-commit git show --stat per the shared-tree discipline). Do NOT touch
   docs/journals/ yet.

## HOLD for my FINAL go (do NOT do yet)
- phase-18 fold-in (the round is still LIVE: @@Alex is testing + found an editor
  bug @@LaneA is fixing; more journal entries are still landing).
- ALL deletions: .claude, .codex, docs/archive, the 8 redirect cards,
  bootstrap.md, skills/ subdirs, and docs/journals LAST. I give the go once the
  editor bug is fixed, @@Alex is done exploring, and the final gate is green.

## Why partial
Deleting docs/journals now would destroy the live team bus while we're still
coordinating fixes. Stage everything; I sequence the deletions as the true
final step. Cut task-LaneF-Lead-3.md when the fold-17 + scrubs are committed +
you're staged-and-waiting on deletions; poke me (and separately poke me the
coordination.md diff for sign-off).
