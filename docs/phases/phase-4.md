# Phase 4 - sparse bug-bounty placeholder (numbering skip)

Status: sparse, never ran as a worked phase
Span: no development span (single import commit on 2026-05-18)
Versions: none
Tags: #docs

## Roadmap (the asks)

There was no recorded request. The stated intent at import time was a bug-bounty / random-updates bucket. That bucket never received entries and no request.md was ever written.

## Rounds and waves

Phase 4 did not run. It is a numbering skip: the project's active sequence jumps from phase 3 directly to phase 5. The directory was created on 2026-05-18 by a single migration commit that imported an early-form process template and an empty bugs.md file.

## Team and coordination

No agents were assigned and no work was recorded. The imported process.md defined a roster (architect plus backend, frontend, webtest, syseng, rustacean lanes) and a coordination scheme based on markdown files with ownership by @@mention, but those were template role definitions, not a record of anyone working this phase. Agent handles and roles are documented in ../agents/README.md.

## What shipped, tried, and undone

Nothing shipped under the phase 4 name. The directory held only three files: an early-form process template, an empty bugs.md, and an import note. Work that might have landed here was absorbed into adjacent phases.

## Retrospective

There is no execution to assess. The one observation worth preserving is a documentation drift pattern: the top-level journals index at various points described phase 4 as "missing", "import pending", and as having "never had a directory", all of which the same-day import note already contradicted. The lesson is minor but real: placeholder directories should carry a one-line README at creation time so that later readers do not mistake absence of content for absence of a directory.

## Notes

Terminology: process.md predates the per-author-directory layout adopted in phase 7. The coordinate-by-mention scheme it describes is an early form of the task-file ownership model the project settled on later.

The raw working material (process template, bugs.md, import note) is preserved in git history under docs/journals/phase-4/; that tree was removed from the working tree in the phase-15 docs cleanup.
