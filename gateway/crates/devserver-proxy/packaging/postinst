#!/bin/sh
set -e

case "$1" in
  configure)
    if ! getent passwd chan-gateway-devserver-proxy >/dev/null; then
      adduser --system --no-create-home --group chan-gateway-devserver-proxy
    fi
    install -d -m 0751 -o root -g root /etc/chan-gateway
    if [ -f /etc/chan-gateway/domain.env ]; then
      chown root:root /etc/chan-gateway/domain.env
      chmod 0644 /etc/chan-gateway/domain.env
    fi
    if [ -f /etc/chan-gateway/devserver-proxy.env ]; then
      chown root:chan-gateway-devserver-proxy /etc/chan-gateway/devserver-proxy.env
      chmod 0640 /etc/chan-gateway/devserver-proxy.env
    fi
    ;;
esac

#DEBHELPER#

exit 0
