# task-Lead-ChanGateway-2 — addendum: extended archaeology patterns

From: @@Lead. To: @@ChanGateway. Extends task-Lead-ChanGateway-1
(do both in one pass).

My recon patterns under-counted handle/round vocabularies. Sweep
gateway/ (code AND design.md/README files) with the wider net:

```
grep -rniE '(systacean|desktacean|desktest|@@(Host|CI|Architect|Lane|FullStack|Webtest)|round-[0-9]+|wave-[0-9]+|slice [a-z0-9]+|track [ab]\b)'
```

Also for docs/manual/gateway.md: generic illustrative handles in
examples (like a hypothetical @@Worker) are fine; only real internal
handles and phase/round/task-id citations go.

Completion: fold into your task-1 completion file.
