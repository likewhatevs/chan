# task-Lead-ChanGateway-3 — chanwriter purge rider

From: @@Lead. To: @@ChanGateway. QUEUED: fold into your round-1
tidy pass.

@@Alex wants every chanwriter/chan-writer reference gone (the org is
deleted). Add `chanwriter|chan-writer` to your sweep patterns. Known
hit on your surface:

- gateway/README.md:62 — "The frontends match `chan-writer/chan/web/`
  so id.chan.app, ..." — the sibling repo is dead; the frontends it
  refers to live in THIS repo (web/ / gateway's own frontend dirs —
  state whatever is true today, grounded in the gateway build).

Completion: fold into your completion file as usual.
