# Local dev env. Copy to packaging/gateway/scripts/dev/.env and fill in.
#
# Required: one OAuth provider. GitHub is the easiest because
# their OAuth app accepts the local CA-backed callback used by this stack.
# Generate a dev OAuth app at:
#     https://github.com/settings/developers
# and use:
#     Authorization callback URL = https://id.localtest.me:17000/auth/github/callback

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional: Google + GitLab. Same callback URL pattern, point at
# id.localtest.me:17000/auth/<provider>/callback.
#GOOGLE_CLIENT_ID=
#GOOGLE_CLIENT_SECRET=
#GITLAB_CLIENT_ID=
#GITLAB_CLIENT_SECRET=

# Postgres connection string. Leave defaulted unless you already
# have something at 127.0.0.1:5432 that conflicts.
DATABASE_URL=postgres://chan:chan@127.0.0.1/chan_gateway
