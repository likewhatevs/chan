{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-drop",
  "description": "Drag-pasteboard read for LOCALLY-served SPA windows only (workspace-*, terminal-*). Deliberately excludes tunnel-* and outbound-*: those webviews render a remote-served SPA, and the macOS drag pasteboard is system-wide and outlives the drag, so a malicious remote could harvest paths the user drags in other applications.",
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "local": true,
  "windows": ["workspace-*", "terminal-*"],
  "permissions": ["allow-read-dropped-paths"]
}
