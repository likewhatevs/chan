<!-- Survey overlay placeholder; the real reply-capable overlay is
     rebuilt in Wave 2 (round-3 @@LaneC).

     A valid no-prop component so TerminalTab.svelte can keep mounting
     `<BubbleOverlay />`. It renders nothing until the rebuild lands. -->
