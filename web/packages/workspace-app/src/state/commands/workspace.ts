// Workspace-settings commands: chan-reports indexing toggle, metadata
// archive import/export. Workspace-only (gate with workspaceOnly).
// Register entries with registerCommands. See state/commands.ts for the
// Command shape and helpers.

import { registerCommands } from "../commands";

registerCommands([]);
