// Graph surface commands: available when a graph tab is the active
// surface. Register entries with registerCommands; gate visibility with
// onSurface(ctx, "graph"). See state/commands.ts for the Command shape
// and helpers.

import { registerCommands } from "../commands";

registerCommands([]);
