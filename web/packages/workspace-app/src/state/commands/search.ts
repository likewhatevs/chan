// Search commands: index rebuild, semantic-search enable, embedding
// model picker. Workspace-only (gate with workspaceOnly). Register
// entries with registerCommands. See state/commands.ts for the Command
// shape and helpers.

import { registerCommands } from "../commands";

registerCommands([]);
