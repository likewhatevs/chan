// Terminal surface commands: available when a terminal tab is the active
// surface. Register entries with registerCommands; gate visibility with
// onSurface(ctx, "terminal"). See state/commands.ts for the Command shape
// and helpers.

import { registerCommands } from "../commands";

registerCommands([]);
