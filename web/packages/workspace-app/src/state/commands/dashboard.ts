// Dashboard surface commands: available when a dashboard tab is the
// active surface. Register entries with registerCommands; gate visibility
// with onSurface(ctx, "dashboard"). See state/commands.ts for the Command
// shape and helpers.

import { registerCommands } from "../commands";

registerCommands([]);
