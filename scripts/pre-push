#!/usr/bin/env bash
# Pre-push gate. Keep the target list in the root Makefile so local
# and CI entry points do not drift.
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

exec make pre-push
