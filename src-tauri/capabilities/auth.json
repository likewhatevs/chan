{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "auth",
  "description": "Sign-in webview at id.chan.app — needs to invoke save_pat after minting a token.",
  "windows": ["auth"],
  "permissions": [
    "core:default"
  ]
}
