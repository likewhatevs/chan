{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "drive",
  "description": "Capabilities for per-drive webview windows: drive-* are locally-supervised chan serves, tunnel-* are remote drives reached over chan-tunnel.",
  "windows": ["drive-*", "tunnel-*"],
  "permissions": [
    "core:webview:allow-set-webview-zoom"
  ]
}
