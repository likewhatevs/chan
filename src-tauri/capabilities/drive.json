{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "drive",
  "description": "Capabilities for per-drive webview windows (label drive-*).",
  "windows": ["drive-*"],
  "permissions": [
    "core:webview:allow-set-webview-zoom"
  ]
}
